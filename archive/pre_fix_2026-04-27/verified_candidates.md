# Verified candidates

Generated by `PYTHONPATH=src WEATHER_EDGE_MARKET_SCAN_PAGES=76 WEATHER_EDGE_REPORT_LIMIT=15 python3 -m weather_edge.main verify-candidates`.

Meaning:
- `PASS` = candidate worth manual verification, not automatic trading permission.
- `PAPER` = useful signal, but fails one or more prudence filters.
- `REJECT` = ignore for now.

Current filters:
- executable EV >= 0.15 using CLOB ask
- preferred ask <= 0.10
- confidence high
- liquidity >= 250
- ICAO station lock preferred
- same-day METAR observations preferred

Output: `reports/verified_candidates.json`.
